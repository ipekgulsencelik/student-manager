type Student = {
    studentName: string,
    instructor: string,
    course: string,
    score: string,
    id: string,
}

type InputProps = {
    course?: string
    instructor?: string
    score?: string
    studentName?: string
}